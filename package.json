{
  "name": "steptrace-local",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local source traceability with keyboard workflows, one-step views, offline loading, and recoverable JSON backups. Session 5.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
