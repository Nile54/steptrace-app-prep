{
  "name": "steptrace-local",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Local source versions, conservative comparison, human review, and JSON backups. Session 3.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
