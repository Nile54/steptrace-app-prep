{
  "name": "steptrace-local",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local source-linked application tasks with versioned JSON backups. Session 2.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
