{
  "name": "steptrace-local",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Local source traceability, confirmed dependencies, explained review, and JSON backups. Session 4.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
